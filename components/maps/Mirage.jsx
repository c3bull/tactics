import {ScrollView, Text, View} from "react-native";

const Mirage = () => {

    return (
        <ScrollView style={{backgroundColor: "#0F1114"}}>
            <Text style={{color: "#FFF"}}>Mirage</Text>
        </ScrollView>
    )
}

export default Mirage